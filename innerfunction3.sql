select USER(),DATABASE();
USE sqldb; SELECT * FROM usertbl; SELECT FOUND_ROWS();
USE sqldb; UPDATE buytbl SET price = price / 2;SELECT ROW_COUNT();
SELECT VERSION();
SELECT SLEEP(5);
SELECT '짠~5초가 지나서 제가보여요';